<div class="col-lg-8 col-lg-offset-1">
  <div class="head-block">
    <h1><%= post.title %></h1>
    <div>
      <%= image_tag post.user.gravatar_url, class: 'gravatar-img' %>
      <div class="blog-meta">
        <% if post.class.name == 'Post' %>
          <i class="glyphicon glyphicon-tags"></i><%= link_to post.post_category.name, post.post_category %><br>
          <hr>
        <% end %>
        <i class="glyphicon glyphicon-user"></i>Stuart
      </div>
    </div>
    <div class="wp-thumbs">
      <span class="glyphicon glyphicon-thumbs-up" aria-hidden="true"></span>
      <%= post.thumbs_up %>
      <span class="glyphicon glyphicon-thumbs-down" aria-hidden="true"></span>
      <%= post.thumbs_down %>
    </div>
    <% if cannot? :edit, post %>
      <% if post.class.name == 'Post' %>
        (<%= link_to "Edit", edit_post_path(post) %>)
      <% else %>
        (<%= link_to "Edit", edit_page_path(post) %>)
      <% end %>
    <% end %>
  </div>
  <hr>
  <div class="wp-content" itemprop="articleBody">
    <%= post.formatted_content %>
    <br>
    <br>
  </div>
</div>

<div class="col-lg-2">
  <%= render partial: 'shared/sidebar' %>
</div>